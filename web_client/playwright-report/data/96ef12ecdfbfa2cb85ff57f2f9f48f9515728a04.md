# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: login.spec.ts >> should login successfully and redirect to dashboard
- Location: tests\login.spec.ts:3:5

# Error details

```
Error: expect(locator).toContainText(expected) failed

Locator: locator('h1')
Expected substring: "Welcome back"
Timeout: 5000ms
Error: element(s) not found

Call log:
  - Expect "toContainText" with timeout 5000ms
  - waiting for locator('h1')

```

```yaml
- heading "Sign In to EduFlow" [level=3]
- paragraph: Enter your email and password to access your campus.
- text: Email Address
- textbox "e.g. principal@college.edu"
- text: Password
- textbox "••••••••"
- checkbox "Remember Me" [checked]
- text: Remember Me
- link "Forgot Password?":
  - /url: "#"
- button "Sign In"
- paragraph:
  - text: New to EduFlow?
  - link "Sign up":
    - /url: /register
- alert
```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  | 
  3  | test('should login successfully and redirect to dashboard', async ({ page }) => {
  4  |     // 1. Navigate to the login page
  5  |     await page.goto('/login');
  6  | 
  7  |     // 2. Verify we are on the login page
  8  |     await expect(page).toHaveTitle(/EduFlow/);
> 9  |     await expect(page.locator('h1')).toContainText('Welcome back');
     |                                      ^ Error: expect(locator).toContainText(expected) failed
  10 | 
  11 |     // 3. Fill in the credentials
  12 |     await page.fill('input[type="email"]', 'admin@eduflow.com');
  13 |     await page.fill('input[type="password"]', 'adminpassword');
  14 | 
  15 |     // 4. Click the login button
  16 |     await page.click('button[type="submit"]');
  17 | 
  18 |     // 5. Verify redirection to dashboard
  19 |     await page.waitForURL('/dashboard');
  20 |     await expect(page).toHaveURL(/.*\/dashboard/);
  21 | 
  22 |     // 6. Verify dashboard content loads
  23 |     await expect(page.locator('h1')).toContainText('EduFlow');
  24 | });
  25 | 
```